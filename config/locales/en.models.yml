en:
  dictionary:
    name: &name Name

  activerecord:
    models:
      user:
        one: User
        other: Users

    attributes:
      user:
        name: &name
        email: Email address
        password: Password
        password_confirmation: Repeat password
